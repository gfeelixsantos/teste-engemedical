// Public App Router entrypoint for the SFTP dashboard API.
// The implementation remains beside the SFTP feature so existing imports and
// focused module tests continue to use the same handlers.
export { GET, POST } from '@/app/sftp-integracao/api/route';
